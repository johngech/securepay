import 'package:flutter/material.dart';

class SecurepayApp extends StatelessWidget {
  const SecurepayApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(body: Center(child: Text('Securepay App'))),
    );
  }
}
